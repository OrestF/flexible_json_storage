<%= form_with(model: api_bin, local: true) do |form| %>
  <% if api_bin.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(api_bin.errors.count, "error") %> prohibited this api_bin from being saved:</h2>

      <ul>
      <% api_bin.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
